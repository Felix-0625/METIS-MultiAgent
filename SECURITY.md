# METIS 安全策略

## 1. 支持范围

当前维护目标为最新主开发分支。历史开发快照不提供独立安全维护承诺。

安全修复只有在合入目标分支并完成测试后才视为可用；本地修改、审计结论或未部署代码不代表生产环境已修复。

## 2. 漏洞报告

不要在公开 Issue、讨论区、日志或截图中披露可利用细节、账号、Cookie、Token、API Key、数据库连接串或用户数据。

优先通过以下私密渠道联系仓库所有者：

1. GitHub Private Vulnerability Reporting（若新仓库已启用）。
2. 仓库所有者明确提供的私密联系方式。

报告应包含：

- 受影响版本或提交。
- 受影响接口、文件或功能。
- 必要的复现条件。
- 实际影响。
- 已脱敏的日志或证据。
- 建议修复方向（可选）。

未公布正式安全联系人前，不要猜测或使用非官方邮箱。

## 3. 敏感信息

禁止提交：

- `.env` 和生产配置。
- LLM、SMTP、GitHub、Gitee 等 API Key。
- JWT、Cookie、密码和数据库连接串。
- `METIS_DATA_ENCRYPTION_KEY`。
- 用户数据、项目工作区、数据库文件和生产日志。

要求：

- 密钥通过 Railway Variables 或等效密钥管理服务注入。
- 生产密钥使用独立强随机值。
- 查询接口只返回掩码配置。
- 日志不得输出请求认证头、Cookie 或完整外部响应凭据。
- 泄露后立即吊销并轮换，不能只删除 Git 中的当前文件。

## 4. 生产安全基线

| 范围 | 要求 |
|---|---|
| 数据库 | PostgreSQL；`REQUIRE_DATABASE_URL=true` |
| 持久化 | `METIS_DATA_DIR` 指向受控持久卷 |
| 认证 | 强 `JWT_SECRET`；HttpOnly、Secure Cookie |
| 加密 | 配置 `METIS_DATA_ENCRYPTION_KEY` |
| 网络 | HTTPS/WSS；严格 `CORS_ORIGINS` |
| 代理 | 仅信任明确的反向代理网段 |
| 限流 | 分离登录、读取和写入额度 |
| 请求体 | 设置 `MAX_BODY_SIZE` |
| 管理员 | 强初始密码，首次登录后修改 |
| 数据恢复 | 定期备份 PostgreSQL 和 Volume |

## 5. 权限边界

- 所有项目接口必须校验项目所有者。
- 普通用户只能访问自己的项目、成员、专家和技能数据。
- 管理接口必须校验管理员角色。
- WebSocket 必须校验 Origin、身份和项目所有权。
- 文件路径必须限制在项目工作区内。
- Agent 只能修改任务合同授权的文件。
- 文件责任变更必须记录来源和历史。

## 6. 生成代码执行

METIS 可构建和运行模型生成的代码，该能力属于高风险边界。

- 只在隔离工作区或容器运行。
- 不向生成项目传递平台生产密钥。
- 不挂载宿主敏感目录。
- 限制 CPU、内存、网络、执行时间和子进程。
- 运行验收失败时默认阻止签收。
- 未配置安全隔离时保持 `RUNTIME_ACCEPTANCE_ENABLED=false`。

## 7. 外部集成

- 模型供应商、SMTP、GitHub、Gitee 和 MCP 均视为外部信任边界。
- 使用最小权限凭据。
- 对外部超时、限流和认证失败采用失败关闭。
- 不把第三方返回文本直接当作可信命令、路径或验收证据。
- 本地模拟测试不能证明真实供应商权限和生产网络正确。

## 8. 依赖与发布

发布前至少执行：

- Python 测试。
- TypeScript 类型检查和前端构建。
- Bandit 或等效静态安全检查。
- 依赖漏洞扫描。
- API 鉴权、用户隔离和路径穿越回归。
- 数据库迁移与重启恢复验证。
- 部署环境健康检查。

安全修复应保持最小变更，并增加对应回归测试。

## 9. 已知安全限制

- 多副本共享文件资源池尚未完成并发一致性验证。
- 运行生成代码无法仅靠应用级校验消除风险，必须依赖基础设施隔离。
- Railway 实际 Volume 权限、数据库 TLS、代理头和 WebSocket 行为需要在线验证。
- 当前仓库存在大量本地重构；切换仓库前必须审查纳入范围，排除临时工具和测试产物。
